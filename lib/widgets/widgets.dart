import 'package:flutter/material.dart';

class Widgets {
  static Widget emptyStream() =>
      const Center(child: Text("no stream connection"));

  static Widget noData() => const Center(child: Text("no data"));
}
